# 積み残しタスク管理 (VRC-BoardGameKit)

本ドキュメントは、プロジェクト内の実装タスク、懸念点、および優先順位を一元管理する台帳です。
機能実装や設計判断が完了するたびに、AIが自動更新・同期を行います。

---

## 1. タスク一覧（ステータス管理）

| ID | タスク名 | 分類 | 優先度 | 担当 | 状態 | 備考 |
| :---: | :--- | :---: | :---: | :---: | :---: | :--- |
| **T01** | プロジェクト憲章・規約の策定 (`AGENTS.md`) | ドキュメント | 高 | AI/User | **完了** | 2026/09/16策定 |
| **T02** | 技術知見・数理ノートの初期化 (`STUDY.md`) | ドキュメント | 高 | AI | **完了** | 2026/09/16策定 |
| **T03** | Git管理初期化 (`.gitignore`, `git init`) | 環境構築 | 高 | AI | **完了** | 初期化完了 |
| **T04** | カード＆デッキ同期マネージャー (`DeckManager.cs`) | U#実装 | 高 | AI | **完了** | Manual Sync, Fisher-Yates |
| **T05** | テーブル＆全体進行マネージャー (`TableManager.cs`) | U#実装 | 高 | AI | **完了** | プレイヤー・手札統括、委譲モデル |
| **T06** | プレイヤー手札トレイ＆秘匿制御 (`HandTrayController.cs`) | U#実装 | 高 | AI | **完了** | 本人のみ表面表示・ローカルマテリアル切替 |
| **T07** | 座席システム連動 (`SeatController.cs`) | U#実装 | 中 | AI | **完了** | VRCStation 着席・離席連動 |
| **T08** | 操作UIコントローラー (`TableUIController.cs`) | U#実装 | 中 | AI | **完了** | ドロー・シャッフル・配布・パスボタン |
| **T09** | ルール拡張プラグイン雛形 (`RulePluginBase.cs`) | U#設計 | 高 | AI | **完了** | イベントフックの定義 |
| **T10** | Local LLM用ルール生成プロンプトテンプレート作成 | ドキュメント | 高 | AI | **完了** | docs/Local_LLMルール生成ガイド.md 作成 |
| **T11** | VPMパッケージング定義 (`package.json`) の作成 | パッケージ | 中 | AI | **完了** | VCC/VPM対応マニフェスト作成 |
| **T12** | 3Dプレハブ化・Prefabファースト配置システム | エディタ/Unity | 高 | AI | **完了** | CardTable_4Players.prefab、UdonSharpProgramAsset完全同期 |
| **T13** | 座席着席インタラクト＆UIフィードバック正常化 | U#/エディタ | 高 | AI | **完了** | VRCStation着席、Immobilize、ステータステキスト、ログ出力 |
| **T14** | UdonSharpEditorUtility完全準拠・U#シリアライズ同期 | U#/エディタ | 高 | AI | **完了** | AddUdonSharpComponent, CopyProxyToUdon, テストプロジェクト同期 |
| **T15** | コンパイルエラー8件の解消とAPI仕様是正 | エディタ修正 | 高 | AI | **完了** | AddUdonSharpComponent拡張メソッド化、不正asset削除 |
| **T16** | UIボタン無反応の解消＆文字化け対策 | U#/エディタ | 高 | AI | **完了** | UIButtonHandler二重化、Canvas余計Collider削除、文字セット最適化 |
| **T17** | デバッグ用クリック判定検証ボタンの実装 | 検証ツール | 高 | AI | **完了** | 3Dキューブ型＆UI型の独立デバッグボタン生成機能 |
| **T18** | ユーザー調整TransformのPrefab同期＆コード逆反映 | Prefab/コード | 高 | AI | **完了** | 視認性向上のための手札トレイ・山札位置をパッケージ＆コードへ完全固定化 |
| **T19** | U#現場技術規範の策定とTMP標準化規程の確立 | 規約/ドキュメント | 高 | AI | **完了** | docs/UdonSharp実装規約.md 策定、Interact原則・TMP NotoSansJP標準化を憲章連携 |
| **T20** | オブジェクトクリックでプレイエリアにつく（非Station化） | U#/仕様 | 高 | AI/User | **完了** | 椅子に座る（Immobilize）ではなく、クリックでプレイエリアと連動・参加登録。ClientSim検証完了 |
| **T21** | プレイエリア連動時のプレイヤー専用UI表示 | U#/UI | 高 | AI | **完了** | 案A（手元パーソナルUI）採用。DEBUG_UI_Canvas仕様に準拠したWorldSpace UI Canvasを手元に配置完了 |
| **T22** | カードオブジェクトの視認性向上（両面カード作成） | Prefab/Shader | 高 | AI | **完了** | 1.2mアバター最適化(70cm×98cm大判Quad)。VRマクロ(SPS-I)対応両面シェーダー(CardTwoSided)、裏面左右反転補正、実画像(幼女/ura2)プレハブ生成完了 |
| **T23** | カードの立て札（カードスタンド/スロット保持機構） | ギミック/Prefab | 高 | AI | **完了** | CardSnapZone.cs実装完了。トリガー検知型スナップ枠、接近時ハイライト発光、Zファイティング防止（ガイド非表示）対応 |
| **T24** | 残り山札枚数のリアルタイム表示機能 | U#/UI | 中 | AI | **完了** | 視線ホバー時のInteractionText動的残数表示（Zero-Traffic HUD）へ一本化。Draw/Reset時にリアルタイム自動更新 |
| **T25** | 磁石型カードプレイエリア（グリッドスナップ配置機構） | ギミック/U# | 高 | AI | **完了** | CardPickupHandler.cs実装完了。完全Kinematic運用、空中完全静止、AutoHold=No設定 |
| **T26** | Tell, Don't Ask原則に基づくカード・スナップ枠リファクタリング | U#設計/リファクタ | 高 | AI | **完了** | 外部直接Transform代入全廃、CardController自己完結移動・空中静止、CardSnapZone受入判定・命令発行モデル確立 |
| **T27** | ポータブル4大AIエージェント体制の確立 (`agents/`) | AI設計/体制 | 高 | AI | **完了** | Architect, Coder, UdonInspector, Refactorerの4規範策定、U#20項目チェックリスト確立、他ツール移植性確保 |
| **T28** | プレイヤー包囲型円弧スロット ＆ 着席排他制御 | U#/空間設計 | 高 | AI | **完了** | 半径1.4m円弧配置、チルト下端収束対策(12度)、重なり完全排除・下端隙間8cm確保、最短距離・重なり判定(Best Fit Snap)による隣接誤吸着完全防止、着席時動的出現、二重着席完全防止 |
| **T29** | 円弧スロット幾何パラメータのSSOT化・可変対応リファクタリング | 設計/リファクタ | 高 | AI | **完了** | ArcadeFieldConfig構造体新設、マジックナンバー完全排除、CreateOptimized(count)による枚数別自動最適化（3/4/5/7枠メニュー対応） |
| **T30** | 一元幾何方程式への統一と専用EditorWindow（スライダーGUI）の実装 | 機能追加/UI | 高 | AI | **完了** | if文段階分岐を完全撤廃し視野角・弦長から連続逆算する一元方程式へ統一、ArcadeFieldBuilderWindow新設によりスライダー(1〜10枠)で自由生成可能に |
| **T31** | 正式リリース時のmainマージ＆GitHubデフォルトブランチ復帰 | リリース | 中 | AI/User | **未着手** | Ver 1.0完成時にdevelop→mainへPRマージ、GitHub Default branchをmainへ切り替え・タグ発行 |
| **T32** | オブジェクトプール方式による山札＆カード正式実装 | U#/ギミック | 高 | AI | **完了** | 側面メッシュレス表裏Quad山札、全カード実体事前プール(5〜54枚可変)、連続ドロー手元順次配備、リセット回収循環を完全確立 |
| **T33** | 山札プール総数・表裏テクスチャ一括GUI設定ツール | エディタ/GUI | 高 | AI | **完了** | DeckCardEditorWindow新設。プール枚数動的増減、全カード表裏統一、複数選択一括割当、連番D&D一括流し込みを完全実装 |
| **T34** | 手元パーソナルUIドローボタン（DrawCardButton）の実装 | U#/UI | 高 | AI | **完了** | DEBUG_UI_Canvas仕様（WorldSpace Canvas+VRCUiShape+BoxCollider）の手元ドローボタンを各席SlotContainerへ自動生成 |
| **T35** | Tools拡張メニューのスリム化＆レガシー不要ロジック完全削除 | エディタ整理 | 中 | AI | **完了** | 旧型円卓・クイックプリセット・デバッグ生成・旧Prefabを完全削除。2大GUIウィンドウ＋2大ユーティリティのスリム体系に集約 |
| **T36** | カードプレハブ整理 ＆ CardController改修 (Step 1) | U#/Prefab | 高 | AI | **完了** | CardSlotController除去、VRCPickup.pickupable=false、Interact/SetSelectedVisual追加、TableManagerスタブ連携完了 |
| **T37** | TableManager即時モード (Immediate) 実装 (Step 2) | U#実装 | 高 | AI | **完了** | BOOTH版基本動作。1クリック即時プレイ、CardSnapZoneスタック配列(allowStack/2mmオフセット/Zファイティング防止)対応完了。スロット解放・所属記憶のTell原則化(SnapToZone)によるドロー満杯不具合の解消完了 |
| **T38** | 複数選択管理 ＆ 15cm浮上演出の実装 (Step 3) | U#実装 | 高 | AI | **完了** | 開発版・役判定用。TableManagerにisCardSelected管理、ToggleCardSelection(Tell命令)、ClearAllSelections、場カードガード節を実装完了 |
| **T39** | 手元UIへの「PLAYボタン」追加 ＆ 一括プレイ実装 (Step 4) | U#/UI | 高 | AI | **完了** | PlayCardButton新設、手元UI横並び2ボタン化、3D物理ボタン(Interact)へ完全移行。FIFOキュー（クリック順）一括プレイ実装。カード基準位置自己更新抽象化、場札ガード、スロット180度反転修正、動作確認完了 |
| **T40** | ドキュメント同期 ＆ レガシーコード整理 (Step 5) | ドキュメント | 中 | AI | **完了** | STUDY.md更新、旧CardSlotController完全削除、Builder配列整理 |
| **T41** | レガシー手札管理の残存調査と新仕様への完全統合（技術負債解消） | リファクタ/調査 | 中 | AI | **完了** | TableManager/SeatControllerのHandTray依存完全削除、PersonalHandArea(ClearAllSlots/GetFirstEmptySlot)へ一本化、HandTrayControllerファイル削除 |
| **T42** | 状態変数（Runtime State）のシリアライズ除外とカプセル化（リファクタリング） | リファクタ | 中 | AI | **完了** | CardSnapZone(isOccupied/currentCard)のSerializeField完全撤廃、CardController(currentZone/candidateZone/isSelected)のprivate化と公開メソッド(GetCurrentZone/IsSelected/ClearZone)整備、状態遷移Debug.Log出力化、dotnet build 0エラー検証済み |
| **T43** | 手元ドローボタン実行時の山札オブジェクト残数表示同期 ＆ 手元UI残数表示 | U#/UI | 中 | AI | **完了** | 案A採用。DrawCardButtonにbuttonText(TMP)連携、DeckManager.UpdateVisuals一元通知、プロキシ代入抜け是正完了。dotnet build 0エラー検証済み |
| **T44** | カード現在位置に基づく処理判断の抽象化 ＆ MultiSelect位置更新バグ修正 | U#設計/バグ修正 | 高 | AI | **完了** | CardZoneType導入、IsInHand/IsOnFieldによる抽象ガード化、PlaySingleSelectedCardスナップ先行トランザクション化、巻き戻り完全解消、dotnet build 0エラー検証済み |
| **T45** | KISS/YAGNI原則に基づくコード簡素化と全クラスのカプセル化統一 | リファクタ | 高 | AI | **完了** | 死にモデル（CardZoneType等）、二重ガード、未使用オーバーロード/デッドフィールド/動的自己修復を完全削除。全クラスのInspector設定変数を[SerializeField] private＋プロパティ/セッターへ統一。dotnet build 0エラー検証済み |
| **T46** | ソフトウェア開発原則の体系化とArchitect事前監査フローの策定 | ドキュメント/体制 | 高 | AI/User | **完了** | SOLID/KISS/YAGNI/DRYの優先不等式（KISS >= YAGNI > DRY > SRP/ISP > OCP）明文化、3倍則・過剰堅牢化禁止規定、Plan策定時の01. Architect AI原則監査・矛盾エスカレーション制度化 |
| **T47** | 離席時の手札カード残存解消（カード非表示または山札回収処理） | U#/バグ修正 | 中 | AI | **未着手** | カードドロー後に離席した場合、スロットコンテナは非表示になるが、吸着されているカードオブジェクトが空中に表示されたまま残存する不具合の修正 |
| **T48** | 複数枚バリデーション基盤と検証サンプルの導入 (Phase 1) | U#設計/実装 | 高 | AI/User | **完了** | `RulePluginBase` 複数枚フック (`CanPlayCards`/`OnCardsPlayed`)、`TableManager` バリデーション・動的フォールバック、`OnPlayerSeated/Left` 復元、`SamplePairOnlyPlugin` 導入 |
| **T49** | ゲームステート管理および座席連携の本格統合 (Phase 2) | U#実装 | 高 | AI/User | **完了** | 手札集計・ファサードAPI、手番制御、終局ロック、物理トリガー連携（TableActionTrigger）の先行検証完了 |
| **T50** | オリジナルカードゲーム実装とパッケージング (Phase 3) | U#実装/パッケージ | 高 | AI/User | **未着手** | 具体的なルールプラグイン実装（大富豪、ブラックジャック等）、`.asmdef` クリーンアップ、BOOTH配布用 `.unitypackage` ビルド |
| **T51** | T49実装内容のToolsビルダー（CardTableBuilder.cs）への落とし込み・反映 | エディタ/リファクタ | 高 | AI/User | **完了** | TestProjectで先行検証したT49構成（TableManagerファサードAPI、DrawCardButton/DeckInteractHandlerのTableManager連携、TableActionTriggerリセットボタン自動生成、SamplePairOnlyPlugin勝利手番制御）を本プロジェクトおよびCardTableBuilderへ完全同期。dotnet build 0エラー確認済み |

---

## 2. 総合テスト・多人数同期検証一覧（Integration & Multiplay Tests: Mシリーズ）

単体機能の枠を超え、他プレイヤーとの相互作用やネットワーク同期伝播を検証する総合テスト台帳です。

| ID | テスト項目名 | 分類 | 優先度 | 担当 | 状態 | 備考 |
| :---: | :--- | :---: | :---: | :---: | :---: | :--- |
| **M01** | 多人数プレイ環境における同期・動作影響の検証（総合テスト） | 多人数同期 | 中 | AI/User | **未着手** | 多人数プレイの影響は未テスト（※他人のドローボタンを操作できない排他制御は **T43** で実装済み。ClientSim複数人/実機環境での総合同期テストを行う） |

---

## 3. 状態の定義
*   **未着手**: まだ作業を開始していない状態
*   **進行中**: 現在作業中の状態
*   **レビュー待ち**: 実装完了し、ユーザー確認待ちの状態
*   **完了**: 動作確認およびドキュメント同期が完了した状態
*   **保留**: 仕様検討や外部要因により一時停止している状態

---

## 3. 次回対応予定（Next Action - T47 / T50 / M01）

### 概要
T51（先行検証構成のTools自動生成・コード完全同期）が完了したため、以下のいずれかを実施予定：
*   **T47: 離席時の手札カード残存解消**: 離席時にスロット上のカードが非表示または回収される処理の実装
*   **T50: オリジナルカードゲーム実装とパッケージング (Phase 3)**: ルールプラグイン実装（大富豪、ブラックジャック等）、BOOTH配布用 `.unitypackage` ビルド
*   **M01: 多人数プレイ環境における同期・動作影響の検証（総合テスト）**: ClientSim複数人/実機テスト

---

## 4. 作業引き継ぎ書（各ステップの詳細計画 ＆ 動作確認手順）

### 🔹 Step 1 (T36): カードプレハブの整理 ＆ CardController改修
*   **対象ファイル**:
    *   `Assets/Projects/Scripts/Core/CardController.cs`
    *   `Assets/Projects/Scripts/Editor/CardTableBuilder.cs` (`BuildYoungGirlCardPrefab`)
    *   `Assets/Projects/Prefabs/Card_01_YoungGirl.prefab`
*   **作業内容**:
    1. `Card_01_YoungGirl.prefab` から `CardSlotController` を完全除去。
    2. `VRCPickup.pickupable = false` に設定。
    3. `CardController.cs` に `TableManager` 参照、`Interact()`（`tableManager.OnCardClicked(this)` 呼び出し）、`SetSelectedVisual(bool selected)`（選択フラグ保持と、通常位置 ↔ 上方15cm浮上の見た目切り替え）を追加。
*   **🔍 動作確認方法**:
    *   Unityでコンパイルエラー（Console赤文字）がないこと。
    *   `Card_01_YoungGirl.prefab` の Inspector で `CardSlotController` が消え `CardController` 単体であること、`VRCPickup.pickupable` が `false` であることを確認。

---

### 🔹 Step 2 (T37): TableManager の即時プレイ（Immediate Mode）実装 【BOOTH版の成立】
*   **対象ファイル**:
    *   `Assets/Projects/Scripts/Core/TableManager.cs`
    *   `Assets/Projects/Scripts/Editor/CardTableBuilder.cs`
*   **作業内容**:
    1. `TableManager.cs` に `CardPlayMode` enum（`Immediate`, `MultiSelect`）を定義（デフォルト: `Immediate`）。
    2. `TableManager.cs` に `OnCardClicked(CardController card)` を実装。
    3. 即時モード時: クリックされたカードの `cardId` を受け取り、中央プレイエリアへ即座に移動（`PlayCard` 連携）させる処理を実装。
    4. `CardTableBuilder.cs` でカード生成時に `TableManager` 参照を自動バインド。
*   **🔍 動作確認方法**:
    1. `Tools -> VRC-BoardGameKit -> Dynamic Arcade Field Builder` から空間を自動生成。
    2. ClientSim で Play ➔ 参加登録キューブをクリックして着席 ➔ 手元の「カードを引く (DRAW)」ボタンでドロー。
    3. 手元スロットのカードに視線を合わせてクリック（左クリック）。
    4. **【期待結果】**: カードが即座に中央の `Center_PlaySlot` へピタッと移動・整列し、手元スロットが空くこと。

---

### 🔹 Step 3 (T38): 複数選択管理 ＆ 15cm浮上演出の実装 【開発版・役判定の準備】
*   **対象ファイル**:
    *   `Assets/Projects/Scripts/Core/TableManager.cs`
*   **作業内容**:
    1. `TableManager.cs` に選択状態を追跡する配列 `bool[] isCardSelected` を追加。
    2. `ToggleCardSelection(CardController card)` を実装（選択フラグ反転 ＆ `card.SetSelectedVisual(isNowSelected)` 呼び出し）。
    3. `ClearAllSelections()`（一括選択解除）の実装。
*   **🔍 動作確認方法**:
    1. シーン上の `TableManager` の Inspector で `playMode` を `MultiSelect` に変更。
    2. ClientSim で Play ➔ 着席してカードをドロー。
    3. カードをクリック ➔ **手前上方に約15cmスッと浮上** することを確認。
    4. もう一度クリック ➔ **元のスロット位置にスッと戻る** ことを確認。
    5. 複数枚を同時に浮上状態にできることを確認。

---

### 🔹 Step 4 (T39): 手元UIへの「PLAYボタン」追加 ＆ 一括プレイ処理の実装
*   **対象ファイル**:
    *   `Assets/Projects/Scripts/Core/PlayCardButton.cs` (新規作成)
    *   `Assets/Projects/Scripts/Core/TableManager.cs`
    *   `Assets/Projects/Scripts/Editor/CardTableBuilder.cs` (`CreatePersonalUICanvas`)
*   **作業内容**:
    1. `TableManager.cs` に `PlaySelectedCards(int seatIndex)` を実装（選択中の全カードをまとめて場に出し、選択解除）。
    2. 手元操作パネル（パーソナルUI）に「カードを出す (PLAY)」ボタンを追加。
    3. `PlayCardButton.cs` をボタンにアタッチし、クリック時に `TableManager.PlaySelectedCards` を呼ぶよう結線。
*   **🔍 動作確認方法**:
    1. `Dynamic Arcade Field Builder` から空間を再生成（手元UIに「DRAW」と「PLAY」の2ボタンが並んでいることを確認）。
    2. ClientSim で Play ➔ カードを2枚ドロー ➔ 2枚ともクリックして浮上（選択中）にする。
    3. 手元UIの「カードを出す (PLAY)」ボタンをクリック。
    4. **【期待結果】**: 選択していた2枚のカードが中央エリアへ一括でプレイされ、選択状態が解除されること。

---

### 🔹 Step 5 (T40): ドキュメント同期 ＆ レガシーコード整理 【完了】
*   **実施内容**:
    1. `STUDY.md` にセクション55を追加し、不要コード特定4基準と完全整理プロセスを記録。
    2. 初期検証用であった `CardSlotController.cs`（および `.asset`, `.meta`）をパッケージ本体およびテスト環境から完全削除。
    3. `CardTableBuilder.cs` の `EnsureAllProgramAssets()` 配列から `CardSlotController.cs` を除去。

---

### 🔹 技術負債解消 (T41): レガシー手札管理の残存調査と新仕様への完全統合 【完了】
*   **背景**:
    *   初期プロトタイプ時代に作られた `HandTrayController.cs`（数値IDベースの木製トレイ）への依存コードが、`TableManager.cs` 内に複数残存（`DrawCardForPlayer`, `PlayCard`, `DealCardsToAll`, `ResetGame` 等）。
    *   新仕様（`PersonalHandArea` ＆ `CardSnapZone` の3D物理空間スロット）との間で手札枚数や状態通知の断絶が生じ、二重管理の温床となっていた。
*   **実施内容**:
    1. `SeatController.cs` から `linkedHandTray` フィールドおよび `GetLinkedHandTray()`、着席・離席時のオーナー呼び出しを完全除去し、`linkedHandArea` へ一本化。
    2. `TableManager.cs` から `handTrayControllers` 配列宣言を削除。
    3. `TableManager.DrawCardForPlayer(seatIndex)` を、座席の `PersonalHandArea.GetFirstEmptySlot()` へ `deckManager.DrawCardForZone()` する新仕様へ刷新。
    4. `TableManager.DealCardsToAll` を、座席ごとの `DrawCardForPlayer` 呼び出しにシンプルに統合。
    5. `TableManager.ResetGame()` を、各座席の `linkedHandArea.ClearAllSlots()` を呼んで全手札スロットを一括解放する処理へ更新。旧 `PlayCard(seatIndex, slotIndex)` を削除。
    6. `PersonalHandArea.cs` に `ClearAllSlots()` を新設。
    7. `CardTableBuilder.cs` の `EnsureAllProgramAssets()` 配列から `HandTrayController.cs` を除去。
    8. 役目を終えた `HandTrayController.cs`（および `.asset`, `.meta`）をパッケージ本体およびテスト環境から完全削除。
    9. `dotnet build` による事前コンパイル検証（0エラー）完了。

---

### 🔹 リファクタリング (T42): 状態変数（Runtime State）のシリアライズ除外とカプセル化 【完了】
*   **背景**:
    *   `CardSnapZone.cs`（`currentCard`, `isOccupied`）や `CardController.cs`（`currentZone`, `candidateZone`, `isSelected`）等の実行時ステートに `[SerializeField]` や `public` が付与されていたため、UnityエディタのUIイベント（uGUI Buttonクリック時のEventSystem再描画等）によってインスペクターのシリアライズ初期値で上書き・巻き戻しが発生し、マルチ挙動（一括プレイや手札スロット認識）の不整合や混乱の温床となっていた。
*   **実施内容**:
    1. **シリアライズの完全排除**:
       - `CardSnapZone` の `isOccupied`, `currentCard` から `[SerializeField]` を完全撤廃し `private` 化。
       - `CardController` の `currentZone`, `candidateZone`, `isSelected` を `private` 化し、インスペクターに露出する項目を静的設定（名前、幾何閾値、マテリアル等）のみに限定。
    2. **公開メソッド（Getter & Command）の整備（Tell原則）**:
       - `CardController` に `GetCurrentZone()`, `IsSelected()`, `ClearZone()` を新設。外部直接代入を禁止し、座標計算・所属記憶を不可分に行う `SnapToZone` および `ClearZone` 経由に一本化。
       - `TableManager.cs` および `CardSnapZone.cs` からのフィールド直接アクセスをすべて公開メソッド経由へ整流化。
    3. **状態遷移デバッグログの強化（可視化）**:
       - `CardSnapZone` のカード受入（`TrySnap`）、カード解放（`ReleaseCard`）、スタッククリア（`ClearStack`）時に詳細な色付き `Debug.Log` を出力し、インスペクター非表示でもConsole上でスロット占有状況をリアルタイムに100%追跡可能に整備。
    4. **事前コンパイル検証**:
       - `Assembly-CSharp.csproj` および `Assembly-CSharp-Editor.csproj` の双方で `dotnet build` による `0 エラー` を確認済み。

---

### 🔹 機能改善 (T43): 手元ドローボタン実行時の山札オブジェクト残数表示同期 ＆ 手元UI残数表示 【完了】
*   **背景**:
    *   手元ボタン（`DrawCardButton`）からカードを引いた際、山札内部の残数（`deckTopIndex`）は減算されているが、山札オブジェクト（`DeckObject`）のホバー残数テキスト（`UpdateInteractionText`）への更新通知が実行されず、表示が古いままになる現象が発生していた。
    *   さらに、手元の操作パネル上でも山札の残り枚数を即座に確認したいという要望（案A）に対応。
*   **実施内容**:
    1. **C#プロキシ代入抜けの根本是正**:
       - `CardTableBuilder.cs` において、`deckManager.SetInteractHandler(deckHandler)` および `deckManager.SetCardPool(poolCards)` を明示的に呼び出し、直後の `CopyProxyToUdon` による初期値null上書きトラップを根本解決。
    2. **手元ドローボタンへの残数表示統合（案A）**:
       - `DrawCardButton.cs` に `[SerializeField] private TextMeshPro buttonText;` および `UpdateRemainingCount(int remainingCount)` を新設。
       - 山札ホバーテキストと同じ文言ルール（`カードを引く (残り: X枚)` / `山札なし (0枚)`）をボタン表面TMPおよびホバー `InteractionText` に動的反映。
    3. **単一真実源（SSOT）に基づく通知の一元化**:
       - `DeckManager.cs` に `[SerializeField] private DrawCardButton[] handDrawButtons;` を新設。
       - `UpdateVisuals()` 内で山札ホバー（`interactHandler`）と全席の手元ドローボタン（`handDrawButtons`）の双方へ一括通知するアーキテクチャへ統合。
    4. **CardTableBuilderでの自動結線**:
       - 手元ボタン生成時に `drawTmp` を `drawUdon.buttonText` にバインド。
       - 全席の `DrawCardButton` を `deckMgr.handDrawButtons` に一括バインドし、生成直後の初期残数も即時反映。
    5. **事前コンパイル検証**:
       - `dotnet build` により Runtime / Editor 双方で `0 エラー` を確認済み。

---

### 🔹 バグ修正・リファクタリング (T44): カード現在位置に基づく処理判断の抽象化 ＆ MultiSelect位置更新バグ修正 【完了】
*   **背景**:
    *   `TableManager` の `OnCardClicked` にて、`centerPlayZone` との一致を直接判定する個別具体的なガード節が存在しており、特定ゾーンへの密結合（過剰な堅牢化・抽象化不足）となっていた。
    *   MultiSelect（複数選択）モード実行時、カードの現在位置（`currentZone` / `normalPosition`）の更新フローが交錯し、場に出たカードをクリックした際に手札トグル処理が誤爆して手札スロットへ帰ってきてしまう致命的バグが発生していた。
*   **実施内容**:
    1. **ゾーン種別（CardZoneType）モデルの導入**:
       - `CardSnapZone.cs` に `CardZoneType` Enum（`Hand`, `Field`, `Discard`, `Deck`）を新設。
       - `CardController.cs` に `IsInHand()` および `IsOnField()` を新設。
       - `TableManager.cs` の `OnCardClicked` および `ToggleCardSelection` の特定ゾーン比較を全廃し、`!card.IsInHand()` による「**手札ゾーン（Hand）にあるカードのみ操作・選択可能**」という抽象ガードへ一本化。
    2. **一括プレイトランザクションの整流化（不可分処理）**:
       - `PlaySingleSelectedCard` において、`centerPlayZone.TrySnap(card)` が成功したことを確認してから旧手札枠を解放（`ReleaseCard`）する安全なトランザクション設計を採用。
       - `TrySnap` 内部で `card.SnapToZone` が走り、カードの基準位置（`normalPosition`）・姿勢・所属ゾーン（`currentZone = centerPlayZone`）が場の座標へ不可分に確定更新されるため、場札クリック時の巻き戻りバグを根本遮断。
    3. **DeckInteractHandlerのフォールバック整理 ＆ Builder設定同期**:
       - `DeckInteractHandler.cs` 内の旧仕様フォールバック分岐を削除し、`PersonalHandArea` 経由のドロー処理に一本化。
       - `CardTableBuilder.cs` で中央スロットに `CardZoneType.Field`、手札スロットに `CardZoneType.Hand` を明示設定。不要な `using UnityEngine.UI;` を削除。
    4. **事前コンパイル検証**:
       - `test/Test Project` にて `dotnet build` を実行し、Runtime・Editor双方で `0 エラー` を確認済み。

---

### 🔹 バグ修正 (T47): 離席時の手札カード残存解消（カード非表示または山札回収処理）
*   **背景**:
    *   プレイヤーがカードをドローした後に座席を離席（参加登録キューブを再クリック）した場合、`linkedHandArea` のスロット枠（`slotContainer`）は非表示（`SetActive(false)`）になるが、スロットに吸着されているカードオブジェクト（`PoolCard_xx`）は非表示にならず、空中に浮いたまま表示され続けてしまう。
*   **作業内容**:
    1. 離席時（`SeatController.LeaveSeat` または `TableManager.OnPlayerLeft`）における手札カードのライフサイクル仕様の策定（案A: 離席時はスロットと一緒にカードも一時非表示にし、再着席で再表示 / 案B: 離席時に手札のカードを山札へ自動回収・リセット）。
    2. 決定した仕様に基づくカード表示制御または山札回収ロジックの実装。

---

### 🔹 総合テスト (M01): 多人数プレイ環境における同期・動作影響の検証
*   **背景**:
    *   **T43** の実装により、他人のドローボタンを操作できない排他制御（`linkedSeat.GetSeatedPlayerId() == localPlayer.playerId`）や、山札の未着席ドロー遮断は成立した。
    *   一方で、複数人が同時に着席・ドロー・プレイを行った際のManual Sync同期パケット（`RequestSerialization` / `OnDeserialization`）の伝播や競合、山札残数UIのリアルタイム追従性については、複数クライアント環境での実機・ClientSimテストが未実施である。
*   **作業内容**:
    1. ClientSimの複数プレイヤーシミュレーション、または複数アカウントによる実機VRChat環境での動作テスト。
    2. 他人がカードを引いた際、全席の手元ドローボタン残数表示および山札ホバーテキストが遅延なく同期更新されるかの検証。
    3. 同時にドローやプレイを連打した際の排他・同期ズレ（レースコンディション）の有無を検証。

---

### 🔹 機能拡張 (T48): 複数枚バリデーション基盤と検証サンプルの導入 【WebGemini Phase 1 完了】
*   **背景**:
    *   大富豪やポーカー等の複数枚出し（ペア、役等）に対応するため、`RulePluginBase` への複数枚判定フックの追加と、`TableManager` によるバリデーション・イベント通知の統合を実施。
*   **実施内容**:
    1. **`RulePluginBase.cs` の複数枚API拡張**:
       - `CanPlayCards(int playerId, int[] cardIds)`（デフォルト: `true`）の仮想メソッドを追加。
       - `OnCardsPlayed(int playerId, int[] cardIds)` の通知イベントを追加。
       - 全メソッドにXMLドキュメントコメントを整備し、構文をプロジェクト既存のブロック記法に統一。
    2. **`TableManager.cs` のルール委譲組み込み ＆ 参照フォールバック**:
       - `PlayCardImmediate` および `PlaySelectedCards` にて、プレイ前に `activeRulePlugin.CanPlayCards` を呼び出し。違反時はプレイを中断（複数選択時は `ClearAllSelections` で手元へ復帰）。
       - プレイ成功時に `activeRulePlugin.OnCardsPlayed` を発行。
       - `Start()` にて `activeRulePlugin` がInspector未設定の場合に `GetComponentInChildren<RulePluginBase>()` で同一・子階層から自動取得するフォールバックを実装。
       - `SeatController` から呼び出される `OnPlayerSeated` / `OnPlayerLeftSeat` のスタブメソッドを復元。
    3. **動作検証用サンプル `SamplePairOnlyPlugin.cs` の導入**:
       - カードが2枚ちょうどの時のみプレイを許可する最小ルールプラグインを実装。
       - コアから独立した `DynamicCardField_4Players/SampleRule` オブジェクトへ配置する疎結合運用を確立。
*   **🔍 動作検証チェックリスト**:
    1. Unity EditorのPlayModeを開始。
    2. 座席に着席し、カードを3枚ドロー。
    3. **1枚出し**: 1枚選択して「PLAY」➔ 場に出ず手元復帰、ログ「`カードは2枚同時に選択して出す必要があります。`」
    4. **2枚出し**: 2枚選択して「PLAY」➔ 中央場へ2枚整列配置、ログ「`プレイヤー(...)が2枚のカードを出しました: ID X, Y`」
    5. **3枚出し**: 3枚選択して「PLAY」➔ 場に出ず手元復帰。
    6. **サンドボックス切り替え**: `activeRulePlugin` を `None` に設定した場合、1枚出し・複数枚出しが無制限に実行できること。

---

### 🔹 機能拡張 (T49): ゲームステート管理および座席連携の本格統合 【WebGemini Phase 2 - 進行中】
*   **背景**:
    *   Phase 1 で複数枚判定基盤が成立したため、ゲームの進行（ターン制、着席イベント、勝敗判定）を本格的に統合する。
*   **作業状況**:
    1. **`SeatController` の着席/離席イベントと `TableManager` の完全連動 【完了】**:
       - `SeatController` の着席/離席時に `tableManager.OnPlayerSeated(seatIndex, newPlayerId)` / `tableManager.OnPlayerLeftSeat(seatIndex, oldPlayerId)` を呼び出し。
       - `TableManager.GetPlayerSeatIndex(int playerId)` を新設し、プレイヤーIDから着席座席インデックスを安全に逆引き解決可能に整備。
    2. **手番（ターン）進行制御とルールプラグイン側 `CanPlayerAct` バインド 【完了】**:
       - `RulePluginBase.cs` に行動権限判定フック `CanPlayerAct(int seatIndex, int playerId)` を新設（デフォルト: `true` でサンドボックス互換）。
       - `TableManager.PlaySelectedCards()` 内で、カードプレイ前に `activeRulePlugin.CanPlayerAct(localSeatIndex, localPlayerId)` を検証し、手番外プレイヤーのアクションを安全にブロック＆選択解除。
       - 検証用プラグイン `SamplePairOnlyPlugin.cs` にて、`CanPlayerAct` をオーバーライドし、現在の手番座席（`currentTurnSeatIndex == seatIndex`）に着席中のプレイヤーのみプレイを許可するターン制制御を実装・検証完了。
    3. **勝敗判定（`CheckWinCondition`）の定期ポーリングまたはイベント駆動トリガーの整備 【未着手】**:
       - 手札が尽きたプレイヤーの判定や、勝者確定時のゲームステート遷移（`gameState = 2`, `winnerPlayerId`）、およびUIフィードバックの組み込み。









